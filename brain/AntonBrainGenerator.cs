using System;

internal static class AntonBrainGenerator
{
    internal static void GenerateBrains()
    {
        while (true)
        {
            //Generate population
            //Evalutate population
            //Set brain via BrainManager.SetBrain(new AntonBrain(weights, someOtherParamterYouCouldNeed, andAnotherOne))
        }
    }
}